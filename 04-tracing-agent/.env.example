# =============================================================================
# Sample 04 · Tracing — environment file
# Copy this file to ".env" (gitignored) and fill in the values below.
# =============================================================================

# --- Microsoft Foundry project ---
# Find at: Foundry portal → your project → Overview
# Format:  https://<resource>.services.ai.azure.com/api/projects/<project-name>
FOUNDRY_PROJECT_ENDPOINT=https://<YOUR_FOUNDRY_RESOURCE>.services.ai.azure.com/api/projects/<YOUR_PROJECT_NAME>

# Name of a model deployment in your Foundry project (e.g., "gpt-4o").
FOUNDRY_MODEL=gpt-4o

# --- Application Insights for tracing ---
# Find at: Foundry portal → your project → Manage → Tracing
#          → "Connect a new Application Insights resource"
# REQUIRED for this sample — without it, no telemetry is exported.
APPLICATIONINSIGHTS_CONNECTION_STRING=
